# owner-signal-repository-ledger

OwnerSignal contract for privileged repository-ledger configuration.

The ordinary event/query surface lives in `signal-repository-ledger`; this crate
is for owner-only mutation of repository policy.
